export enum MemberType {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETE = "DELETE",
}

//Enums let you define a set of named constants, so you don’t have to use plain strings everywhere in your code.
